# Step 6 约束片段：中英混合技术分享（可整体嵌入系统说明）

以下内容须与所选 `Knowledge/Styles/` 并存；冲突时以更具体的 Style 条目为准。

- 第一人称「我」，口吻自然。
- 删除口头禅、无意义重复、明显跑题；**不删**有信息量的重复强调。
- 保留观点、案例、数据；说不清处保留 `[?]` 或由 Step 7 标 `REVIEW`。
- **专业术语保留英文原词**；中英相邻处加空格。
- 禁止使用视频口播首尾套话（见 `Context/PREFERENCES.md`）。
- 输出**纯 Markdown 正文**：不要 YAML frontmatter，除非用户在 `format-output` 明确要求。
