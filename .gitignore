# 本地密钥与运行时配置（勿提交）
.video2blog.env
.env
*.pem

# 项目核心产物目录（全部不入库，只作为本地工作区）
work/
output/
inbox/
input/Video/*.mp4
input/Video/*.mov
input/Video/*.mkv

__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.so
*.egg-info/
dist/
build/
.venv*
venv/
.venv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# PyInstaller 后端二进制产物（Phase 3 sidecar，太大不入库）
.build-backend/
# stage 进 Tauri resources 的冻结后端副本（make stage-sidecar 生成）
frontend/src-tauri/backend/

# 系统与编辑器
.DS_Store
.claude/
.codex/
# .cursor/ 整体忽略（IDE 状态），但 skills/ 是引擎承重合同（Step 3–8 的 SKILL.md），必须入库
.cursor/*
!.cursor/skills/
.qoder/
.idea/
.vscode/
*.swp
*.swo
*~

# memory 运行产物（CONFIG.md / PREFERENCES.md 仍入库作为 workflow 契约）
memory/HISTORY.md
memory/fingerprints.jsonl

# few-shot 范文只留本地，不入公开仓库；README 仍入库作为目录说明
knowledge/Examples/*.md
!knowledge/Examples/README.md

