# AGENTS.md

This repository uses `WORKFLOW.md` as the single runtime workflow contract.

For any video-to-blog or transcript-to-blog task:

1. Read `WORKFLOW.md` first.
2. Follow its Pre-Flight, `ENTRY`, `MODE`, `ROUTING`, `SOURCE`, and Step rules.
3. Do not duplicate workflow rules here; update `WORKFLOW.md` instead.

`AGENTS.md` is only an adapter for agent tools.
